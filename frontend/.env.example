# Backend API base URL.
# Leave empty in dev — Vite's dev proxy in vite.config.ts forwards /api/* to localhost:8080.
# Set to production backend URL in Cloudflare Pages environment variables:
#   VITE_API_BASE_URL=https://mycellis-backend.fly.dev

VITE_API_BASE_URL=
